# Database Configuration
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=carbon_intensity
DB_PORT=5432

# Note: DB_HOST is automatically set to 'postgres' (the service name) inside Docker
# If you need to access the database from your host machine, use localhost:5432